import "./Users.scss";

const Users = () => {
  return (
    <div className="users">
      <div className="info">
        <h1>Users</h1>
        <button className="Button">Add New User</button>
        <button className="Button">Add New User</button>
        <button className="Button">Add New User</button>
        <button className="Button">Add New User</button>
        <button className="Button">Add New User</button>
        <button>Add New User</button>
        <button>Add New User</button>
        <button>Add New User</button>
      </div>
    </div>
  );
};

export default Users;
